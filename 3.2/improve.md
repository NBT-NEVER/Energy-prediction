# 3.2 改进日志

## 2026-09-07 固定采样与时间感知训练

- 在项目外读取旧原始记录，按 flight 插值到统一 0.12 s 时间网格。新表替换 `D:/Python-files/Energy-prediction/data/dji_matrice_100`，旧不规则数据备份为同级 `dji_matrice_100_irregular_backup`；一次性整理脚本不纳入项目。
- 强制重建项目 `out/data/processed_3.2`。项目读取全部 11 种航线的 322904 条固定采样原始记录；train/val/test 为 221220/48841/52843 条。
- 在原 22 维输入上加入 `dt_seconds`。TCN 的时间门控层按每步真实间隔调节输入，再送入因果卷积。
- TCN 仍输出逐采样点功率。训练 DataLoader 保持 `flight + floor(time)` 的完整秒窗口，损失为逐点功率 Huber 损失加 0.2 倍秒窗口能量 Huber 损失；能量差经积分反向传播到窗口内全部功率输出。
- 修复加入 `dt_seconds` 后输出列重复，导致秒级能量聚合失败的问题；修复 CUDA AMP 下 `scatter_add` 精度不一致的问题。

## RLS 在线机制

- 每个 flight 独立从偏置 0、缩放 1 开始。用 TCN 窗口平均功率 50 W 作为停机/飞行阈值，状态切换时恢复中性参数与初始协方差。
- 当前窗口先使用旧参数校正功率；窗口结束后才获得真实能量。RLS 回归量使用未经校正的 TCN 窗口能量，监督目标使用真实窗口能量，更新参数仅作用于下一窗口。
- 删除 RLS warmup 参数选择。所有候选均在完整窗口结束后立即更新，兼容字段 `warmup_windows` 固定为 0；不完整末窗口不参与更新。
- 价值函数增加 flight 能量误差 95 分位和 flight 间标准差惩罚，降低单个 flight 过度校正风险。
- 3.2 已完成权重沿用 6 组 RLS 参数搜索结果，实际固定遗忘因子 0.86、初始协方差 1.0，warmup 固定为 0；RLS 能量窗不参与搜索，参考窗为 1 s，另做 1、2、5、10、20 s 变量分析。

## 启动方式与产物

- `python main.py tune-tcn`：只搜索 TCN 并保存最优阶段模型。
- `python main.py train-fixed`：显示当前 TCN 参数，跳过搜索完成正式训练。
- `python main.py tune-rls`：加载现有 TCN，只搜索 RLS 并写回最优参数。
- `python main.py test`：只运行固定模型与 RLS 参数的测试、评估和制图。
- RLS 调参表、参数轨迹、统计、置信区间和参数变化图统一放入 `out/rls`。
- 功率与每秒能量时序图同时展示真实值、纯 TCN 和 RLS 校正值。

## 本次实验结果

- 固定 2.0 s/17 步 TCN 训练 20 轮，搜索范围缩小为单个候选。
- 测试集纯 TCN / RLS 功率 WAPE 为 7.8114% / 6.8892%，RLS 功率 RMSE 43.4546 W、R2 0.9623。
- 测试集纯 TCN / RLS 整 flight 能量 WAPE 为 2.7277% / 0.2923%，RLS 能量 RMSE 0.0890 Wh、R2 0.9996。
- 测试轨迹含 5442 个秒窗口、63 次状态切换。偏置参数均值 0.02107、标准差 0.20328，缩放参数均值 0.95554、标准差 0.20592；参数范围分别为 [-1,1] 和 [0,2]，少量窗口触及裁剪边界。
